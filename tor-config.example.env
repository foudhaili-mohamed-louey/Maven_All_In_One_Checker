# Tor Proxy Configuration Example
# Copy this file to .env or set these as system environment variables

# ============================================
# Tor Proxy Settings
# ============================================

# Enable or disable Tor routing for Holehe and Gravatar
# Set to 'true' to route requests through Tor
# Set to 'false' for direct connections (default)
TOR_ENABLED=false

# Tor SOCKS5 proxy host
# Default: 127.0.0.1 (localhost)
# Change this if Tor is running on a different machine
TOR_HOST=127.0.0.1

# Tor SOCKS5 proxy port
# Default: 9050 (Tor default SOCKS port)
# Tor Browser uses: 9150
TOR_PORT=9050

# ============================================
# Connection Timeouts
# ============================================

# Connection timeout in milliseconds
# How long to wait when establishing connection to Tor
# Default: 30000 (30 seconds)
# Increase this if you experience timeout errors
TOR_CONNECTION_TIMEOUT_MS=30000

# Read timeout in milliseconds
# How long to wait for response data
# Default: 30000 (30 seconds)
# Increase this for slow Tor circuits
TOR_READ_TIMEOUT_MS=30000

# ============================================
# Circuit Rotation
# ============================================

# Circuit rotation interval in milliseconds
# How often to request a new Tor circuit (change exit node)
# Default: 10000 (10 seconds)
# Longer intervals reduce load on Tor network
# Shorter intervals provide more IP rotation
TOR_CIRCUIT_ROTATION_INTERVAL_MS=10000

# ============================================
# Example Configurations
# ============================================

# For Development (Tor disabled, fast):
# TOR_ENABLED=false

# For Production with Tor Browser:
# TOR_ENABLED=true
# TOR_PORT=9150
# TOR_CONNECTION_TIMEOUT_MS=60000
# TOR_READ_TIMEOUT_MS=60000
# TOR_CIRCUIT_ROTATION_INTERVAL_MS=30000

# For Heavy Rate-Limited Services:
# TOR_ENABLED=true
# TOR_CIRCUIT_ROTATION_INTERVAL_MS=5000

# For Slow Networks:
# TOR_ENABLED=true
# TOR_CONNECTION_TIMEOUT_MS=90000
# TOR_READ_TIMEOUT_MS=90000
# TOR_CIRCUIT_ROTATION_INTERVAL_MS=20000

# ============================================
# Notes
# ============================================
# 1. Tor must be installed and running for TOR_ENABLED=true to work
# 2. Circuit rotation requires Tor control port (9051) to be enabled
# 3. All timeout values are in milliseconds (1000ms = 1 second)
# 4. Invalid values will fall back to defaults
# 5. These settings apply to both Holehe and Gravatar requests
